{
  "private": true,
  "scripts": {
    "postinstall": "npm run install-server && npm run install-client",
    "install-server": "cd server && npm ci --only=dev && npm ci --only=prod && npm run build",
    "install-client": "cd client && npm ci --only=dev && npm ci --only=prod && npm run build",
    "start": "cd server && npm run run"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
