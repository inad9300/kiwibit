{
  "private": true,
  "scripts": {
    "postinstall": "npm run install-server && npm run install-client",
    "install-server": "cd server && npm i --only=dev && npm i --only=prod && npm run build",
    "install-client": "cd client && npm i --only=dev && npm i --only=prod && npm run build",
    "start": "cd server && npm run run"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
