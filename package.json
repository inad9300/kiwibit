{
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm run install-server && npm run install-client",
    "install-server": "cd server && npm ci && npm run build",
    "install-client": "cd client && npm ci && npm run build",
    "start": "node server/bin/main.js"
  },
  "devDependencies": {
    "@types/webpack": "4.41.17"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
